# apiVersion: sqs.aws.upbound.io/v1beta1
# kind: Queue
# metadata:
#   name: alpha-queue
# spec:
#   forProvider:
#     region: eu-west-1
#     name: alpha-queue
#   providerConfigRef:
#     name: default
apiVersion: platform.knab-internal.com/v1alpha1
kind: Legacy
metadata:
  name: legacy-alpha-queue
spec:
  name: legacy-alpha-queue